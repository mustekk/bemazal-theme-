/**
 * PostCSS configuration
 *
 * Adds vendor prefixes to CSS rules using autoprefixer.  You can extend this configuration to
 * include additional PostCSS plugins if necessary.
 */
export default {
  plugins: {
    autoprefixer: {},
  },
};