# Быстрая справка по командам

## 🚀 Запуск разработки

```bash
npm run dev
```

Запускает dev сервер на http://localhost:5173

## 🛑 Остановка сервера

### Вариант 1: npm команда (рекомендуется)
```bash
npm run stop
```

### Вариант 2: Shell скрипт
```bash
./stop-vite.sh
```

### Вариант 3: Вручную по порту
```bash
kill -9 $(lsof -ti:5173)
```

### Вариант 4: Убить все процессы Vite
```bash
pkill -f vite
```

## 🏗️ Сборка production

```bash
npm run build
```

Создает оптимизированные файлы в папке `dist/`

## 📦 Установка зависимостей

```bash
npm install
```

## 🔍 Проверка статуса

### Проверить работает ли Vite
```bash
lsof -ti:5173
```

Если выводится число (PID) - сервер запущен
Если пусто - сервер остановлен

### Посмотреть все процессы Vite
```bash
ps aux | grep vite
```

## ⚙️ Конфигурация WordPress

Добавьте в `wp-config.php`:

```php
// Для режима разработки
define('VITE_FORCE_DEV', true);

// Опционально: изменить адрес dev сервера
define('VITE_SERVER', 'http://localhost:5173');
```

## 🎯 Полезные команды

### Очистить dist папку
```bash
rm -rf dist/
```

### Очистить node_modules и переустановить
```bash
rm -rf node_modules package-lock.json
npm install
```

### Проверить версию пакетов
```bash
npm list --depth=0
```

### Обновить зависимости
```bash
npm update
```
